import Block from "./UI/Block/Block";
import Button from "./UI/Button/Button";
import Input from "./UI/Form/Input/Input";

const App = () => {
  return (
    <div className="App">
      <Block flex flexOption={{ direction: "column", gap: "1rem" }}>
        S
      </Block>
    </div>
  );
};

export default App;
